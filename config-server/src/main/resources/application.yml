server:
  port: 8888
spring:
  application:
    name: config-server
  profiles:
    active: dev
  cloud:
    config:
      label: master
      server:
        git:
          uri: https://github.com/flyer21/military-config-rep.git
#          searchPaths: SpringcloudConfig
#          username: flyer21@163.com
#          password: frf12345
eureka:
#  instance:
#    hostname: localhost
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
#  profiles:
#debug: true
#    active: dev
management:
  endpoints:
    web:
      exposure:
        include: '*'
